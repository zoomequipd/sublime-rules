name: "24 hour cancellation time"
type: "query"
source: | 
  type.inbound
  and (
    strings.ilike(body.current_thread.text, "*24 hours*")
    or any(attachments,
           any(file.explode(.), strings.ilike(.scan.ocr.raw, "*24 hours*"))
    )
  )
severity: "low"
