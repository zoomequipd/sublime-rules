name: "Sender display name matches logo in screenshot"
type: "query"
source: | 
  any(ml.logo_detect(beta.message_screenshot()).brands,
    strings.icontains(.name, sender.display_name)
  )
severity: "high"