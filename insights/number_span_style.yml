name: "Phone number displayed using styled HTML span tag"
type: "query"
source: | 
  type.inbound
  and regex.icontains(body.html.raw,
                      '<span\sstyle="color.{0,25}">.*(?:\d.*?){10,15}</span>'
  )
severity: "low"
