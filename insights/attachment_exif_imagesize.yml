name: "Exiftool: ImageSize"
type: "query"
source: | 
  map(attachments,
      map(file.explode(.),
          map(filter(.scan.exiftool.fields,
                     .key == "ImageSize"
              ),
              .value
          )
      )
  )
severity: "informational"
